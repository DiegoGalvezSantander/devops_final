apiVersion: v1
kind: Service
metadata:
  name: svc-back-despachos
  namespace: innovatech-production
spec:
  type: ClusterIP
  selector:
    app: back-despachos
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080